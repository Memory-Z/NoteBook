<?xml version="1.0" encoding="utf-8"?>

<!-- 新笔记 添加 图片  -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/dp120"
    android:layout_height="@dimen/dp120">

    <ImageView
        android:id="@+id/item_nnii_iv"
        style="@style/Base.ImageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"
        android:contentDescription="@string/_image"
        android:src="@drawable/img_phone_1" />

    <ImageView
        android:id="@+id/item_nnii_close_iv"
        style="@style/Base.ImageView.Icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_marginTop="@dimen/dp02"
        android:layout_marginEnd="@dimen/dp02"
        android:background="@drawable/bg_button_radius"
        android:contentDescription="@string/_close"
        android:src="@drawable/ic_close_black_24dp"
        android:tint="@color/white" />
</RelativeLayout>