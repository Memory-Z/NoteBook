<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/Base.Layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="@dimen/margin_text"
    android:paddingBottom="@dimen/margin_text">

    <TextView
        android:id="@+id/item_search_content_tv"
        style="@style/Base.Text.Title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="@dimen/margin_text_layout"
        android:layout_toStartOf="@id/item_search_content_close_iv"
        android:ellipsize="end"
        android:lines="1" />

    <ImageView
        android:id="@+id/item_search_content_close_iv"
        style="@style/Base.ImageView.Icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:contentDescription="@string/_image"
        android:src="@drawable/ic_close_black_24dp" />

</RelativeLayout>